spring:
  config:
    activate:
      on-profile: dev

---
spring:
  config:
    activate:
      on-profile: dev

server:
  port: ${PORT:8080}

datasource:
  url: jdbc:postgresql://localhost:5432/neverlucky
  username: neverlucky_user
  password: BJs1RxxJzuQ1G4wsGmIbToHcUpQ6WMvn
  driver-class-name: org.postgresql.Driver
  hikari:
    maximum-pool-size: 5
    minimum-idle: 2
    connection-timeout: 20000
  
jpa:
  hibernate:
    ddl-auto: update
  show-sql: false
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      format_sql: true
  open-in-view: false

jwt:
  secret: ${JWT_SECRET:MinhaChaveJWTUltraSeguraComMaisDe32Caracteres123456}
  expiration: ${JWT_EXPIRATION:86400000}

frontend:
  url: ${FRONTEND_URL:https://neverluckycardgame.vercel.app}

logging:
  level:
    org.hibernate.SQL: info
    org.springframework.web: info

---
spring:
  config:
    activate:
      on-profile: prod

# Production configuration will be loaded from application-prod.yml
